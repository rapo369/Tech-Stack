apiVersion: v1
kind: Service
metadata:
  name: np-app-service
  labels:
    name: app
spec:
    type: NodePort
    ports:
    - port: 80
      protocol: TCP
      targetPort: 80
    selector:
      name: app

